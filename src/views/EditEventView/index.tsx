'use client'
import { EventResponse } from '@/types/dataModel/event';

interface EditEventViewProps {
    event: EventResponse;
}

export default function EditEventView({ event }: EditEventViewProps) {
    
    return (
    )
}

